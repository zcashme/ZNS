[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "zcashname-sdk"
version = "0.4.0"
description = "Python SDK for the Zcash Name System (ZNS) JSON-RPC API"
requires-python = ">=3.10"
license = "MIT"
dependencies = ["httpx>=0.27"]
readme = "README.md"

[project.urls]
Repository = "https://github.com/zcashme/ZNS"
